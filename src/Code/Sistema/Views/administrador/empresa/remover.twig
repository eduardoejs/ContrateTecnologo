{% extends 'layout.twig' %}

{% block links %}
{% endblock %}

{% block conteudo %}
<br><br><br>
<div class="row">
    <div class="col-lg-12">
        <h2>Remover Empresa</h2>
    </div>
</div>

<div>
  <ol class="breadcrumb">
    <li><a href="{{ path('index-admin') }}">Painel do Administrador</a></li>
    <li><a href="#">Módulos</a></li>
    <li><a href="{{ path('list-empresa') }}">Empresa</a></li>
    <li class="active">Remover</li>
  </ol>
</div>

<div class="row">
    <div class="col-lg-12"> 
        <form action="{{ path('delete-empresa', {'id': empresa[0].id}) }}" method="post" class="form-horizontal">

            <div class="form-group">
                <label>Nome da empresa</label>
                <input type="text" name="nome" class="form-control" disabled="disabled" value="{{ empresa[0].nome }}">
            </div>
            
            <input type="hidden" name="id" value="{{ empresa[0].id }}">
            <input type="hidden" id="_method" name="_method" value="DELETE" />
            <button type="submit" class="btn btn-info"><i class="glyphicon glyphicon-check"></i> Confirmar</button>
            <a href="{{ path('list-empresa') }}" class="btn btn-default">Cancelar</a>
        </form>
        {% if msg is defined %}            
            <div class="alert alert-danger alert-dismissable row" role="alert">
            <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button><ul>
            {%for erro in msg %}
                <li>{{erro}}</li>
            {%endfor%}
                </ul></div>
        {%endif%}
    </div>
</div>
{% endblock %}

{% block scripts %}
{% endblock %}