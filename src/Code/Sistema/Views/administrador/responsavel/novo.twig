{% extends 'layout.twig' %}

{% block links %}
{% endblock %}

{% block conteudo %}
<br><br><br>
<div class="row">
    <div class="col-lg-12">
        <h2>Responsável da empresa: {{empresa[0].nome}}</h2>
    </div>
</div>

<div>
  <ol class="breadcrumb">
    <li><a href="{{ path('index-admin') }}">Painel do Administrador</a></li>
    <li><a href="#">Módulos</a></li>
    <li><a href="{{ path('list-empresa') }}">Empresa</a></li>
    <li><a href="{{ path('list-empresa-resp',{empresa_id: empresa[0].id}) }}">Responsáveis</a></li>
    <li class="active">Novo</li>
  </ol>
</div>

<div class="row">
    <div class="col-lg-12">
        <form action="{{ path('new-empresa-resp', {empresa_id: empresa[0].id}) }}" method="post" class="form-horizontal">
            <div class="form-group">
                <label>Nome do Responsável</label>
                <input type="text" name="nome" class="form-control" required>
            </div>          
            <div class="form-group">
                <label>CPF</label>
                <input type="text" name="cpf" class="form-control" required>
            </div>            
            <div class="form-group">
                <label>Email</label>
                <input type="email" name="email" class="form-control" required>
            </div>
            <button type="submit" class="btn btn-info"><i class="glyphicon glyphicon-check"></i> Cadastrar</button>
            <a class="btn btn-default" href="{{ path('list-empresa-resp',{empresa_id: empresa[0].id}) }}" role="button">Cancelar</a>
        </form>
    {% if msg.ERROS is defined %}
      <div class="alert alert-danger alert-dismissable row" role="alert">
          <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button><ul>
      {%for erro in msg.ERROS %}
          <li>{{erro.Campo}}</li>
      {%endfor%}
          </ul></div>
    {%endif%}
    </div>
        
</div>
{% endblock %}

{% block scripts %}    
{% endblock %}